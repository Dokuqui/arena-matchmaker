services:
  frontend:
    build: .
    container_name: arena_frontend
    restart: always
    command: ./frontend
    ports:
      - "50051:50051"
      - "2112:2112"
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    # deploy:         # UNCOMMENT THIS TO SHOW OFF SCALING
    #   replicas: 2

  worker_eu:
    build: .
    container_name: arena_worker_eu
    restart: always
    command: ./matchmaker
    environment:
      - REDIS_ADDR=redis:6379
      - REGION=EU-WEST
    depends_on:
      - redis

  worker_us:
    build: .
    container_name: arena_worker_us
    restart: always
    command: ./matchmaker
    environment:
      - REDIS_ADDR=redis:6379
      - REGION=US-EAST
    depends_on:
      - redis

  redis:
    image: redis:alpine
    container_name: arena_redis
    ports:
      - "6379:6379"

  prometheus:
    image: prom/prometheus
    container_name: arena_prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
